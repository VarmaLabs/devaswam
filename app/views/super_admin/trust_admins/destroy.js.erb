<% if @trust_admin %>
  <% if @trust_admin.new_record? %>

  	// Fill the right box with first trust_admin details
  	$('#div_trust_admin_details').html("<%= escape_javascript(render(:partial=>'summary')) %>");

  <% else %>

  	// Render the show partial to show flash message
  	$('#div_trust_admin_details').html("<%= escape_javascript(render(:partial=>'show')) %>");

  <% end %>

  // Render trust_admins List
  $('#div_trust_admin_collections').html("<%= escape_javascript(render(:partial=>'index')) %>");

<% else %>

  var noResultsText = "<div id=\"div_trust_admin_empty\" class=\"box text-color-grey p-80 text-align-center\" style=\"height:200px;\"><%= translate("forms.no_results_found") %></div>";
  $('#div_trust_admin_index').html(noResultsText);
  $('#div_trust_admin_details').html(noResultsText);

<% end %>
